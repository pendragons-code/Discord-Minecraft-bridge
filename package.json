{
  "name": "discord-minecraft-bridge",
  "version": "1.0.0",
  "description": "Discord bot element and backend of the system          The way this works is that it receives a post request from the minecraft element and it sends the username(minecraft) and the message to discord",
  "main": "index.js",
  "dependencies": {
    "better-sqlite3": "^8.0.1",
    "discord.js": "^14.7.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "quick.db": "^9.0.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "node > logfile 2> errfile & exit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
